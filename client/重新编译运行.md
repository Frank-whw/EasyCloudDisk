# 重新编译运行客户端

## 🔧 已修复的问题

- ✅ 临时禁用了秒传检查（因为后端API返回500错误）
- ✅ 现在文件会直接上传，不检查是否存在

## 📝 重新编译步骤

```bash
# 1. 停止当前运行的客户端（Ctrl+C）

# 2. 重新编译
cd client
mvn clean package -DskipTests

# 3. 重新运行
java -jar target/clouddisk-client-1.0.0.jar
```

## ✅ 验证步骤

运行后，在 `client/local/` 文件夹中：

```bash
# 创建或修改文件
echo "测试内容" > test.txt
```

应该看到日志：
```
开始上传文件: ./local/test.txt
使用API上传文件: ./local/test.txt
文件上传成功: ./local/test.txt
```

## ⚠️ 注意事项

**临时方案的影响：**
- ❌ 失去了秒传功能（相同文件会重复上传）
- ❌ 会浪费一些带宽和时间
- ✅ 但是能够正常工作了

**后续需要修复：**
后端的 `/files/check` API 有bug，需要：
1. 查看服务器日志找到具体错误
2. 修复后端代码
3. 重新部署后端
4. 恢复客户端的秒传检查功能



